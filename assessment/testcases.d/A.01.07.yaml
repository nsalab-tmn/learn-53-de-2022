- name: Конфигурация application gateway
  code: s1
  type: check
  result:
  device: local
  action_chain:
    - action: validate_web_response
      action_vars:
        name: "{answer_endpoint_url}"
        assert_code: 200
